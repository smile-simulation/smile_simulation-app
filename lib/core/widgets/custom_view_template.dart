import 'package:flutter/material.dart';

class CustomViewTemplate extends StatelessWidget {
  const CustomViewTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
