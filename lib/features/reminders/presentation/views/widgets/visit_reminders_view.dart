import 'package:flutter/material.dart';

class VisitReminders extends StatelessWidget {
  const VisitReminders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
